# Перенос FinGam CRM на уровень AlfaCRM

## Context

Школа финансовой грамотности переезжает с **AlfaCRM** (`bmk.s20.online`) на собственную FinGam CRM.
К проекту подключился специалист, который вёл клиентов в AlfaCRM — он второй пользователь и источник требований.

Разрыв между системами огромный. AlfaCRM — это учебный центр целиком: 86 клиентов, 168 уроков,
1461 финансовая операция, 11 тарифов, 5 групп, воронка из 6 этапов со 143 лидами.
Наша CRM — «электронная тетрадь»: клиенты, платежи, расходы. Нет ни расписания, ни групп,
ни педагогов, ни посещаемости, ни абонементов, ни лидов, ни касс.

Цель: пошагово закрыть разрыв, не ломая то, что уже работает, и в конце перенести исторические данные.

---

## Что обнаружено в AlfaCRM (результат обследования)

### Ключевая идея, которой у нас нет

**Остаток клиента двойной: в деньгах и в уроках одновременно** — «1 320 000,00 / 4 уроков».
Клиент покупает **абонемент** (пакет уроков). Когда урок проводится, списывается один урок
из пакета и его стоимость из денег. Наш `type:'session'` — лишь бледная тень этого механизма.

### Разделы и их содержимое

**Клиенты** (86). Список: ФИО, возраст + дата рождения, общий остаток (деньги / уроки),
статус обучения, контакты, примечание, дата след. посещения.
Вкладки: Текущие, Я — ответственный, Без группы, С группой, Д/р в этом месяце, Архивные.

Карточка ученика: остаток (уроки + деньги), ID, счётчики «Платежи N шт», «Уроки план/факт»,
«Академ. часы план/факт», педагог, **заказчик** (мама как плательщик, отдельно от ребёнка),
мобильный, **счета и абонементы** (Пакет 8, предмет «Финансовая грамотность», период
26.06.2026–03.09.2026), архивные абонементы, скидки, **аллергии/особенности**, номер школы,
группы (+ архивные), регулярные уроки, предмет и уровень, договоры, загруженные файлы,
**виджет посещений** (цветные плитки по датам), единая лента «Уроки и оплаты».

Действия: Добавить, Править, В архив, Отчёты, Приостановки, Управление скидками,
**История изменений**, **Импорт из файла**, **Экспорт в файл**.

**Уроки** (168). Календарь (день/неделя/месяц) с аудиториями + список.
Статусы: **Проведён / Отменён / Запланирован**. Действия: Подтвердить, Отменить,
Вернуть в запланированные, Удалить.
Поля урока: дата, время (11:00–16:00), длительность (300 минут), участники (группа + поимённо),
педагог, тема. Урок бывает групповой и индивидуальный.
Проведение урока порождает списание: «Групповой — Финансовая грамотность, 300м, Посещение, 330 000».

**Группы** (5). Название, период обучения (22.06.2026 — 01.09.2026), клиенты (0/12 — лимит мест),
расписание (ПН 11:00), архивные группы.

**Абонементы** (11). Шаблоны пакетов: «Пакет 4 (1 300 000,00 за 4 шт)», «Пакет 8 (2 400 000 за 8 шт)»,
варианты «с питанием» и «без питания». Поля: название, тарификация (поурочный),
стоимость, «используется» (сколько выдано / активно), дата добавления. Есть архивные.

**Финансы** (1461 операция). Полноценная касса.
Типы: **Доход #N / Расход #N / Выплата ЗП #N**.
Поля: дата, тип, сумма, **счёт (касса)** — «Xolis (карта)», «Наличные (xolis)», «Карта»;
**статья** — «Оплата за занятие», «Налог 1% ИП», «Зарплата тренера», «Доп услуги»;
назначение (клиент → базовый счёт), **плательщик** («Мохинур мама @mmd02_05»), комментарий.

**Лиды** (143). Канбан-воронка, 6 этапов:
Не разобрано (0) → Установлен контакт (113) → Думают/не отвечают (18) → Назначено пробное (1)
→ Проведено пробное (0) → Получена оплата (11). Отдельно «Архив (отказы)».
Карточка лида: имя (обычно «мама такого-то»), дата, примечание, источник
(Реклама в инстаграм / Рекомендация), контакт (телефон + telegram), ответственный.

**Педагоги**, **Задачи**, **Юр. лица**, **Аналитика** — существуют, в этот заход не изучались.

### Визуальный язык: цвета несут смысл

Виджет посещений в карточке ученика кодирует **12 состояний** плитками с датой.
Правило простое и его надо сохранить: **цвет = деньги, значок = факт**.

| Состояние | Фон | Значок |
|---|---|---|
| Запланирован | серый | — |
| Запланирован бесплатный | жёлтый | — |
| Запланирован без абонемента | белый, пунктирная серая рамка | — |
| Предоплачен | зелёный | — |
| Проведён и оплачен | зелёный | ✓ зелёная |
| Проведён в долг | розовый | ✓ розовая |
| Пропуск в долг | розовый | ✗ красный |
| Бесплатный пропуск | жёлтый | ✗ оранжевый |
| Пропуск оплачен | жёлтый | ✗ красный |
| Забыли провести? | белый, красная пунктирная рамка | ? |
| Приостановлен | серый, текст зачёркнут | ⏸ |
| Отменён | серый, текст зачёркнут | ⊖ |

Зелёный — оплачено. Розовый — долг. Жёлтый — бесплатно. Серый — не состоялось.
Зачёркивание — урок не идёт в счёт. Пунктир — требует внимания администратора.

Та же логика в остальных разделах: в списке уроков строки проведённых уроков подсвечены розовым,
заголовки этапов воронки окрашены по мере продвижения к оплате, статус клиента — серый бейдж «Активен».

**Экраны и кликабельность (проверено):**
- Клик по ФИО в списке клиентов открывает карточку (`/customer/view?id=231`), ФИО — ссылка целиком.
- Кликабельны: педагог, заказчик, телефон, абонемент (карандаш для правки), счётчики
  «Платежи 4 шт» и «Уроки п13/ф13» — ведут в отфильтрованные списки.
- Карточка двухколоночная: слева лента событий и виджет посещений, справа узкая колонка-сводка
  (остаток, ID, педагог, заказчик, абонементы, скидки, аллергии, группы, договоры).
- Списки: пагинация 20 строк, «Настройка полей» — пользователь сам выбирает колонки.
- Наверху каждого раздела одинаковая панель действий: Добавить, Править, Отчёты, Другое, Фильтр.

При реализации: снять точные размеры и отступы карточек с живых экранов, не изобретать свои.

### Карточка урока (клик по плитке посещения)

Открывается компактный поповер прямо поверх карточки клиента. Заголовок — «групповой проведен»
(тип + статус). Позиции:

| Поле | Пример |
|---|---|
| Тип | Групповой (пометка «норм») |
| Время | с 11:00 до 14:00 (180 мин.) |
| Аудитория | *(не задан)* — красным, если пусто |
| Педагог(и) | Гришанович Алексей Артурович — ссылка |
| Предмет | Финансовая грамотность |
| Группы | Группа сб 11 — ссылка |
| Список учеников | 7 строк: значок статуса, ФИО (ссылка), **сумма списания** |

Кнопки внизу: **Открыть**, **Провести** (гаснет, если урок уже проведён), **Вернуть**.

**Самое важное открытие.** У каждого ученика в одном и том же уроке **своя стоимость**:
Камилла 335 000, Ахмедов 250 000, Бендик 290 000, Исматова 380 000, Махмудов 250 000,
София 380 000, Эшимов 300 000.

Причина (уточнено владельцем): **цена занятия у каждого ребёнка своя и вводится вручную.**

- Пакет покупают **не все**. Многие платят поурочно, без всякого абонемента.
- Пакеты мы предлагаем, но они лишь один из способов оплаты, а не основа тарификации.
- У части ребят **персональная цена**, которую менеджер вбивает руками.

Следствие для модели: `amountCharged` — **всегда редактируемое поле**, а не вычисляемое.
Система лишь подставляет разумное значение по цепочке приоритетов:

1. активный абонемент ученика → `price ÷ lessonsCount`;
2. иначе персональная цена ученика (`clients.lessonPrice`);
3. иначе цена по умолчанию для предмета/группы.

Подставленную сумму менеджер может переписать в журнале. Сохраняем **фактическую** сумму
в `lessons.attendance[].amountCharged`, чтобы позднее изменение тарифа не переписало историю.

**Пробное занятие** — тот же механизм, просто по заниженной цене: `type: 'trial'`,
сумму менеджер вводит сам. Отдельной логики бесплатности не нужно.

### Чего у нас нет (сводка разрыва)

| AlfaCRM | FinGam сейчас |
|---|---|
| Абонементы, остаток в уроках | только деньги |
| Уроки, расписание, статусы | нет |
| Посещаемость | нет |
| Группы | нет |
| Педагоги, выплаты ЗП | категория расхода `salary` |
| Кассы и статьи | плоские `amount` |
| Лиды и воронка | поле `source` у клиента |
| Заказчик ≠ ученик | нет |
| ~~Скидки, приостановки~~ | *не переносим — не используются* |
| История изменений | нет |
| Импорт/экспорт | только JSON-бэкап |
| Роли | один UID в whitelist |

---

## Фактическая модель на 9 июля 2026 (реализовано)

```
clients      + lessonPrice, payerType ('parent'|'legal'), legalEntityId, status
               ('active'|'paused'|'dropped'), mother/father { name, phones[], instagram, telegram }
teachers     { name, phone, telegram, rate, active }
packages     { name, lessonsCount, price, active }          // шаблон абонемента
accounts     { name, kind: 'cash'|'card', active }
categories   { name, kind: 'income'|'expense'|'salary', order, active }
legalEntities{ name, contactName, phone, note, active }

groups       { name, teacherId, mode: 'weekly'|'range', weekdays[], dateFrom, dateTo,
               timeFrom, timeTo, studentIds[], archived }
lessons      { groupId, groupName, date 'YYYY-MM-DD', timeFrom, timeTo, teacherId,
               type: 'group'|'individual'|'trial', topic,
               status: 'planned'|'conducted'|'cancelled',
               studentIds[], attendance: [{ clientId, clientName, status: 'present'|'absent',
                                            amountCharged, subscriptionId? }] }
subscriptions{ clientId, packageId, name, lessonsTotal, lessonsUsed, price,
               startDate, endDate, status }
payments     { clientId, clientName, amount, type: 'income'|'session', sessions,
               description, date, lessonId? }               // lessonId нужен для отката
```

Фаза 5 заменит `payments`/`expenses` на `transactions` с кассами и статьями.

## Целевая модель данных (Firestore)

Существующие коллекции `clients`, `payments`, `expenses` сохраняются; `payments` со временем
поглощается коллекцией `transactions`.

```
teachers      { name, phone, telegram, subjects[], rate, active }
subjects      { name, levels[] }                       // «Финансовая грамотность»
packages      { name, lessonsCount, price, active, archived }
                                                       // шаблон: «Пакет 8 (2 400 000 за 8)»
legalEntities { name, contactName, phone, note }       // школа-плательщик (пока один клиент)
groups        { name, subjectId, teacherId, capacity,
                schedule[{ weekday, time, durationMin }],
                startDate, endDate, studentIds[], archived }

subscriptions { clientId, packageId, name, subjectId,
                lessonsTotal, lessonsUsed, price,
                startDate, endDate, status }           // необязателен: многие платят поурочно

// в clients добавляется:
//   lessonPrice    — персональная цена занятия, вводится вручную (может быть пустой)
//   payerType      — 'parent' | 'legal'   (по умолчанию parent)
//   legalEntityId  — если платит юрлицо (школа)

lessons       { date, startTime, durationMin, type: group|individual|trial,
                groupId, teacherId, subjectId, topic,
                status: planned|conducted|cancelled,
                attendance: [{ clientId, status: present|absent|excused,
                               subscriptionId, amountCharged }] }
                                                       // amountCharged — одна сумма,
                                                       // уже включает питание, вводится вручную

leads         { childName, childAge, parentName, phone, telegram,
                source, stage, note, responsibleId,
                createdAt, stageChangedAt, archivedReason }

accounts      { name, kind: cash|card }                // кассы: «Xolis (карта)», «Наличные»
categories    { name, kind: income|expense }           // статьи: «Оплата за занятие», «Налог 1% ИП»
transactions  { date, kind: income|expense|salary, amount, accountId, categoryId,
                clientId, payerName, teacherId, comment }
```

**Главная связь.** Урок переводится в статус `conducted` → для каждого присутствующего
создаётся списание на фактическую сумму. Если у ученика есть активный абонемент,
дополнительно `subscriptions.lessonsUsed += 1`. Если абонемента нет — списываются только деньги,
и баланс может уйти в минус: это и есть долг, ровно как в нынешней CRM.
Отменённый урок не списывает ничего.

Баланс в уроках = `Σ(lessonsTotal − lessonsUsed)` по активным абонементам (у поурочных — просто 0).
Баланс в деньгах = доходы минус списания.

**Питание входит в сумму занятия одной цифрой** (уточнено владельцем).

Менеджер вводит **одну сумму** — стоимость занятия уже с питанием, если ребёнок ел.
Если еду не заказывали, вводится просто стоимость занятия. Именно поэтому суммы у всех разные.

Никаких отдельных позиций, услуг и автоматического сложения. Одно поле, одна цифра, вручную.
Отдельная коллекция `services` **не нужна** — это было бы лишней работой для менеджера.

(Статья «Доп услуги» в финансах остаётся: ею проводят разовые доплаты вроде
«питание остаток за прошлый период», но к журналу урока это отношения не имеет.)

**Скидок и приостановок не будет** — владелец подтвердил, что в работе они не использовались.
Роль скидки де-факто играет персональная цена ученика, которую менеджер вводит вручную.
**Академические часы не ведём** — достаточно счётчика уроков и длительности в минутах.

---

## Статус на 9 июля 2026

| Фаза | Состояние |
|---|---|
| 0. Фундамент | ✅ частично: таймауты запросов, long-polling, ErrorBanner. `writeBatch` применён в группах, уроках, абонементах. Общий data-слой **не сделан** |
| 1. Справочники | ✅ сделано |
| 2. Группы | ✅ сделано |
| 3. Уроки, журнал, виджет | ✅ сделано |
| 4. Абонементы | ✅ сделано |
| 5. Финансы (кассы, статьи, ЗП) | ⬜ следующая |
| 6. Лиды | ⬜ |
| 7. Миграция истории | ⬜ |
| 8. Отчёты и роли | ⬜ |

Плюс сверх плана: интерфейс переведён на светлую тему, список клиентов стал таблицей,
карточка клиента разделена на отдельную страницу `/clients/:id`, добавлен календарь уроков
(месяц/неделя/день) с модалкой проведения.

### Решения, изменившие исходный план

- **Группа = серия занятий**, а не список учеников. Создание группы сразу порождает занятия:
  по дням недели («Группа сб 11») или подряд («Интенсив 7 дней»). В AlfaCRM у всех пяти групп
  колонка «Клиенты» была пуста — группами пользовались именно так.
- **Членство в группе ≠ запись на занятия.** Дети ходят на ближайшие один-два урока, поэтому
  добавление в группу не подписывает на всю серию. На конкретные даты записывают галочками.
- **Питание не отдельная позиция.** Менеджер вводит одну сумму за занятие, уже с питанием.
  Коллекция `services` не нужна.
- **Цена занятия всегда редактируема.** Подставляется по цепочке: абонемент → персональная
  цена ребёнка (`clients.lessonPrice`) → пусто.
- **Расписание группы замораживается** после первого проведённого занятия. Пока проведённых нет —
  дни, период и время меняются свободно, запланированные занятия пересоздаются.
- **Проведённые занятия неприкосновенны.** Правка состава, удаление группы и смена расписания
  их не трогают: за ними стоят списания, иначе поедут балансы.
- **Откат занятия возвращает и деньги, и уроки.** Списания помечены `lessonId`, посещения хранят
  `subscriptionId` — поэтому «Вернуть в запланированные» точно знает, что кому вернуть.
- Предметы, аудитории, оценки, домашние задания и скидки **не переносим**.

### Что осталось заглушками

- «Дата след. посещения» в списке клиентов — колонка есть, значение не считается.
- Педагог в правой колонке карточки клиента — «(не задано)», связи ученик↔педагог нет.
- Оценки, ДЗ, файлы, аудитории — не реализованы сознательно.

---

## План внедрения по фазам

Каждая фаза — самостоятельный работающий кусок, деплоится отдельно.
Порядок продуман так, чтобы справочники появились раньше, чем то, что на них ссылается.

### Фаза 0. Фундамент (полдня)
- Whitelist не трогаем: специалист работает под аккаунтом владельца.
  Когда дойдём до аудит-лога — завести ему отдельный вход, иначе действия не различить.
- Общий data-слой вместо трёх независимых загрузок `payments`
  (сейчас грузится в `Dashboard.jsx`, `Clients.jsx`, `Finance.jsx` целиком).
- `writeBatch` для каскадного удаления клиента (`Clients.jsx:174-180` — сейчас не атомарно).
- Переиспользовать: `src/lib/amount.js` (`toAmount`, `toCount`), стиль форм из `src/components/ClientForm.jsx`.

### Фаза 1. Справочники (1 день)
Новая страница «Настройки»: педагоги, предметы, тарифы (пакеты), кассы, статьи, юрлица.
Простые CRUD-списки по образцу `src/pages/Expenses.jsx`.
Категории расходов, сейчас захардкоженные в `Expenses.jsx:24-31`, переезжают в `categories`.
Среди статей расходов обязательно «Налог 1% ИП» — его вносят руками каждый раз.

### Фаза 2. Группы (1 день)
Страница «Группы»: название, предмет, педагог, лимит мест, период, расписание, состав.
В карточке клиента — блок «Группы».

### Фаза 3. Уроки и посещаемость (2–3 дня) — **сердце системы**
- Страница «Уроки»: список + календарь на месяц.
- Создание урока: разовый и по расписанию группы.
- Журнал: строка на ученика — отметка присутствия и **одна сумма за занятие**
  (редактируемая, подставляется по цепочке приоритетов; уже включает питание).
  У отсутствующих сумма недоступна.
- Персональная цена ученика (`clients.lessonPrice`) добавляется в форму клиента уже здесь —
  без неё журнал придётся заполнять руками каждый раз.
- Тип урока `trial` — пробное, цена заниженная, вводится вручную.
- Статусы `planned → conducted | cancelled`, кнопка «Вернуть в запланированные».
- Проведение урока пишет списания и, если есть абонемент, увеличивает `lessonsUsed` —
  в одной транзакции `writeBatch`.
- **Виджет посещений в карточке клиента — обязателен** (требование владельца).
  Плитки с датами и все 12 состояний из таблицы выше, клик по плитке открывает
  карточку урока: тип, время, педагог, состав группы и сумма списания у каждого ученика.

### Фаза 4. Абонементы (1–2 дня)
Абонемент — **опция**, а не обязательное условие. Поурочная оплата работает без него уже с Фазы 3.

- Выдача пакета клиенту, остаток уроков, срок действия, архив.
- В карточке клиента и в списке — двойной остаток «деньги / уроки», как в AlfaCRM.
  У поурочных учеников показываем только деньги.
- Предупреждение, когда уроки заканчиваются (это и есть «должники к обзвону»).
- Скидки и приостановки не делаем.

### Фаза 5. Финансы (1–2 дня)
- `transactions` вместо плоских `payments`/`expenses`: кассы, статьи, плательщик.
- Тип «Выплата ЗП» с привязкой к педагогу.
- Отчёт по кассам и статьям за период. Метрики Finance пересчитать на новую модель.

### Фаза 6. Лиды (1–2 дня)
- Канбан из 6 этапов, перетаскивание карточек, архив отказов.
- Конверсия лида в клиента одной кнопкой (этап «Получена оплата»).

### Фаза 7. Миграция данных из AlfaCRM (1–2 дня)
Переносим **всю историю с самого начала ведения CRM** — и клиентов, и финансы,
чтобы годовая отчётность не разорвалась. Это решение владельца, объём известен:
86 клиентов, 168 уроков, 1461 операция, 143 лида.

- Выгрузить «Экспорт в файл» по клиентам, платежам, урокам, лидам.
- Скрипт импорта в Firestore, идемпотентный: повторный запуск не плодит дубли
  (ключ — исходный ID из AlfaCRM, хранить в поле `alfaId`).
- Сверка контрольных сумм до и после: число записей и суммы по каждому году.
- Прогон на копии базы, только затем на боевой. Бэкап через `src/lib/backup.js` до начала.
- Исторические уроки импортируются уже проведёнными, повторных списаний не делать.

### Фаза 8. Отчёты и роли (1–2 дня)
- Дашборд «на сегодня»: уроки дня, должники, кончающиеся абонементы, дни рождения.
- Роли: владелец и администратор (сейчас доступ бинарный, ролей нет).
- Экспорт в Excel.

---

## Критические файлы

- `firestore.rules` — whitelist UID, правила для новых коллекций
- `src/App.jsx` — роутинг и меню (пункты продублированы в sidebar и мобильной навигации)
- `src/pages/Clients.jsx` — карточка клиента, сюда добавляются абонементы и группы
- `src/lib/client.js` — хелперы клиента, образец для новых `lib/*.js`
- `src/components/ClientForm.jsx` — образец формы, переиспользовать стили и структуру секций
- `src/pages/Expenses.jsx` — образец простого CRUD для справочников
- `src/lib/backup.js` — бэкап перед миграцией
- Новые: `src/pages/Lessons.jsx`, `Groups.jsx`, `Leads.jsx`, `Settings.jsx`,
  `src/lib/lesson.js`, `subscription.js`, `finance.js`

---

## Проверка

Каждая фаза проверяется вручную на боевом сайте `https://crmbmk-d6303.web.app`
(локальный `npm run dev` ходит в ту же базу — тестовые записи придётся чистить).

Сквозной сценарий после Фазы 4, повторяющий реальную работу школы:

1. Завести клиента с мамой-плательщиком.
2. Выдать «Пакет 8» — остаток становится «2 400 000 / 8 уроков».
3. Создать группу с расписанием, добавить туда клиента.
4. Создать урок на дату, отметить присутствие, провести урок.
5. Убедиться: остаток стал «7 уроков», деньги списались, в ленте клиента появилась запись.
6. Отменить урок — убедиться, что списание откатилось.

После Фазы 7 — сверить: число клиентов 86, сумма операций совпадает с отчётом AlfaCRM.

Регресс после каждой фазы: `npm run build`, `npx eslint src`, вход на сайт, открытие всех вкладок.

Деплой: `VITE_BASE=/ npm run build && npx firebase deploy --only hosting --project crmbmk-d6303`,
правила — `npx firebase deploy --only firestore:rules --project crmbmk-d6303`.

---

## Решения владельца (09.07.2026)

- **Цена занятия** — у каждого ученика своя, менеджер вводит её вручную. Пакеты покупают
  не все: многие платят поурочно. Система подставляет цену (абонемент → персональная →
  по умолчанию), но поле всегда остаётся редактируемым.
- **Питание** — не отдельная позиция. Менеджер вводит одну сумму: занятие уже с питанием,
  если ребёнок ел; иначе просто стоимость занятия. Коллекция услуг не нужна.
- **Пробное занятие** — по заниженной цене, сумму вводит менеджер, питание так же внутри суммы.
- **Плательщик** — обычно родители. Изредка платит **юрлицо (школа)**: сейчас такой клиент один.
- **Налог 1% ИП** — вносится вручную как расход во вкладке «Финансы». Никакой автоматики.
- **Скидки и приостановки** — не нужны, не реализуем.
- **Академ. часы** — не нужны, достаточно уроков и минут.
- **Миграция** — переносим всю историю с самого начала, включая все финансы.
- **Доступ** — специалист работает под аккаунтом владельца; отдельный вход отложен
  до фазы аудит-лога.

## Открытые вопросы

Блокирующих не осталось. По ходу фаз уточнить:

1. Нужна ли пометка «с питанием» у занятия — для отчётности видно было бы, сколько раз кормили.
   Сейчас это не отличить: питание растворено в сумме.
2. Юрлицо-плательщик: нужны ли реквизиты и счета, или достаточно названия школы в карточке.
