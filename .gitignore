# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase env
.env
.env.local

# Firebase deploy cache
.firebase

# Выгрузки из AlfaCRM: ФИО детей, телефоны родителей, суммы платежей.
# Репозиторий публичный — эти файлы не должны попасть в историю git.
data/
*.csv
*.xlsx
*.xls
fingam-backup-*.json

# Временная папка для импорта — персональные данные
public/alfa/

# Ключ сервис-аккаунта Firebase: даёт полный доступ к базе и обходит правила.
# Живёт только в GitHub Secrets и на диске владельца — в git ему нельзя.
serviceAccount*.json
*-firebase-adminsdk-*.json

# Дампы базы: ФИО детей и телефоны родителей. Их место — приватный репозиторий копий.
backups/
